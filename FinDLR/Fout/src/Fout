import java.io.Writer;
import java.io.IOException;
import java.io.File;
import java.util.Scanner;
import java.io.FileWriter;

public class Fout {
		public static void main(String[] args) throws Exception {
			File f = null;
            File f1 = null;
            String v, v1;
            boolean bool = false;
			Scanner scn = new Scanner(System.in);
			
			System.out.println("Enter the file name :");
			Scanner inpt = new Scanner(new File(scn.nextLine()));
			
			System.out.println(input);
			
            f = new File("src_findlr.txt");
			f1 = new File("ThisPC/Documents/JCreator LE/MyProjects/FinDLR");
			// get file name or directory name
            v = f.getName();
            v1 = f1.getName();
         
            // true if the file path exists
            bool = f.exists();
         
            // if file exists
            if(bool) 
         {
            // prints
            System.out.println("File name: "+v);
         }
         
            // true if the directory exists
            bool = f1.exists();
         
            // if the folder exists
            if(bool) 
         {
            // prints
            System.out.print("Folder name: "+v1);
         }
         
          } catch(Exception e) {
            // if any error occurs
            e.printStackTrace();
            fw.close();
        
    }
}